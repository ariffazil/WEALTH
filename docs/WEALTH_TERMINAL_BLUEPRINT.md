# WEALTH Terminal — Repackage Blueprint

> Forged 2026-07-18. Response to sovereign design review (Perplexity, 2026-07-17).
> Verdict accepted: **keep ontology, keep constitutional tone, keep three instruments —
> reframe as one WEALTH terminal with differentiated asset identities and a cross-asset command layer.**
> DITEMPA BUKAN DIBERI.

---

## 0. Code-level confirmation of the review

The review's "sibling clone" diagnosis is not an impression — it is literally how the pages are built:

| Evidence | Receipt |
|---|---|
| All 3 pages are exactly **2207 lines** each | `wc -l /root/sites/{gold,oil,gas}/index.html` |
| Gas page is generated by `cp oil/index.html gas/` + 6 `sed` renames | `/root/sites/setup-commodity-dashboards.sh` lines 11–21 |
| After normalizing asset names, gold↔oil diff ≈ **100 lines of 2207** (~4.5%) | measured 2026-07-18 |
| The 4.5% that differs is exactly the right stuff: drivers, levels, categories | e.g. gold "Physical Flows / ETF" vs gas "Storage / Inventory" |

**EUREKA:** the content ontology already diverges per asset; only the *shell duplication* and *visual identity* don't. So this is a refactor + differentiation job, not a redesign from zero. ΔS strongly negative: 6,621 duplicated lines → ~1 template + 3 config files.

### Current inventory (T₀ 2026-07-18)

```
/root/sites/ == /var/www/html/          ← LIVE webroot, NOT a git repo (F11 gap, see §8)
├── wealth/index.html    959 ln  "Sovereign Valuation Kernel" landing (hero/forge/judge/constellation)
├── gold/index.html     2207 ln  + api/ (gold-api.service, signal_v2.json, fetch_gold.py)
├── oil/index.html      2207 ln  + api/ (oil-api.service  :3457, yfinance BZ=F)
├── gas/index.html      2207 ln  + api/ (gas-api.service  :3458, yfinance NG=F)
└── _shared/design-system, unified-header, trinity-nav, observatory.js

Routes (Caddy, arif-fazil.com): /wealth/ /wealth/gold/ /wealth/oil/ /wealth/gas/ (+ /api/* per asset)
Existing signal schema: signal_v2.json {signal{direction,verdict:SABAR|…,confluence}, regime{ema,rsi}, zones, timestamp}
Page skeleton (identical ×3): macro-ticker → Market Pulse → APEX panel (G, C_dark, ΔS, A/P/E/X/Φ) →
events → chart → signals-grid → physical substrate (rasaDuniaMount) → Synthesis & Decision Gate
(Observe/Prepare/Await) → constellation footer.
```

---

## 1. Target sitemap

```
/wealth/                      WEALTH TERMINAL (home = daily operator surface)
│                             "Capital physics under sovereign judgment."
├── /wealth/gold/             Asset workspace — MONETARY DEFENSE     (reserve trust)
├── /wealth/oil/              Asset workspace — INDUSTRIAL FLOW      (chokepoints & cycle)
├── /wealth/gas/              Asset workspace — OPERATIONAL BALANCE  (storage & seasonality)
├── /wealth/log/              SOVEREIGN LOGBOOK — verdict history, veto events, post-mortems
├── /wealth/api/state.json    Unified machine-readable cross-asset state (wealth.state.v1)
└── /wealth/llms.txt          Agent manifest: schema, routes, parse grammar
```

The existing 959-line landing keeps its narrative role but its hero collapses to a compact
mission band; the terminal grid becomes the body. Public philosophy stays on `arif-fazil.com`
proper — `/wealth/` is the product.

---

## 2. Shared shell (one parse grammar, four canonical sections)

One template + per-asset config replaces three cloned files:

```
sites/wealth/_shell/
├── shell.html.tmpl       single source of the 95% shared skeleton
├── wealth-shell.css      tokens: --asset accent, spacing, type scale
├── wealth-shell.js       data plumbing, NOW card, decision gate, state emitter
└── assets/{gold,oil,gas}.config.json   the 5% that differs, made explicit
```

`{asset}.config.json` (everything the sed script was faking):

```json
{
  "asset": "gold", "name": "Gold", "ticker": "XAUUSD",
  "accent": "#c9a84c", "api_base": "/wealth/gold/api",
  "identity": "MONETARY DEFENSE",
  "first_module": "reserve-trust",
  "driver_categories": ["Monetary / Real Yields", "Geopolitics / Safe Haven",
                        "Physical Flows / ETF", "Risk Sentiment / Equities"],
  "substrate_metrics": ["real_yield_10y", "cb_net_buying", "gsr", "etf_holdings"]
}
```

Reading order fixed — **one dominant NOW card**, everything else demoted:

```
Z1  NOW CARD (dominant)   price + Δ + bias pill + APEX verdict + confluence — merged
                          Market Pulse + APEX header. One glance = current truth.
Z2  MARKET STATE          chart + levels + regime (demoted APEX primitives fold in here)
Z3  CAUSAL DRIVERS        world panels, asset-tuned categories (already differ — keep)
Z4  PHYSICAL SUBSTRATE    asset-specific first module (§3) + rasa dunia
Z5  DECISION GATE         Synthesis, Observe/Prepare/Await, F13 veto block
Z6  FOOTER (compressed)   constellation shrinks to one line: "One causal chain. Three layers."
```

---

## 3. Asset differentiation — the first module IS the identity

| | GOLD | OIL | GAS |
|---|---|---|---|
| Identity | Trust decay / reserve defense | Flow, chokepoint, industrial pulse | Compression, storage, balancing |
| Accent | `#c9a84c` (existing) | `#d1603d` crude amber | `#4dd0e1` flame cyan |
| First module | **Reserve Trust Gauge** | **Flow & Chokepoint Map** | **Storage–Seasonality State** |
| Contents | real 10Y yield, CB net buying, GSR, ETF flows, trust-decay index | Hormuz/Suez status chips, OPEC spare capacity, crack spreads, curve (backwardation/contango) | EU+US storage % vs 5-yr band, HDD/CDD outlook, LNG flows, TTF/JKM/HH spreads |
| Motion metaphor | slow, monumental | linear, directional | seasonal, oscillating |
| Data source (free) | FRED DFII10, WGC monthly, existing yfinance | EIA weekly, existing BZ=F feed | EIA storage report, existing NG=F feed |

Everything below the first module keeps the shared grammar — agents parse one schema,
humans feel three instruments.

---

## 4. Machine layer — `wealth.state.v1`

Each page embeds `<script type="application/json" id="wealth-state">` and the terminal
aggregates to `/wealth/api/state.json`. Extends the existing `signal_v2.json` (superset, no break):

```json
{
  "schema": "wealth.state.v1",
  "ts": "2026-07-18T00:45:00+08:00",
  "assets": {
    "gold": {
      "price": {"last": 4063.40, "delta_pct": 0.05, "levels": {"s": [4020, 3980], "r": [4100, 4150]}},
      "bias":  {"stance": "MILD_BULLISH", "confluence": 0.72},
      "apex":  {"verdict": "SABAR", "G": null, "C_dark": null, "dS": null, "state": null},
      "regime": {"regime": "NEUTRAL", "rsi": 0.0},
      "substrate": {"real_yield_10y": null, "cb_net_buying": null},
      "gate":  {"stance": "OBSERVE", "f13": "ACTIVE", "last_veto": null}
    },
    "oil": {"...": "same grammar, oil substrate keys"},
    "gas": {"...": "same grammar, gas substrate keys"}
  },
  "cross": {"dxy": 103.2, "us10y": null, "vix": null,
            "notes": ["DXY↓ → gold+, gas+ (LNG competitiveness), oil mixed"]},
  "provenance": {"feeds": ["yfinance", "EIA", "FRED"], "generator": "wealth-shell v1"}
}
```

---

## 5. Sovereign Logbook (`/wealth/log/`)

Converts WEALTH from dashboard → institutional memory:

- Nightly cron appends each asset's NOW-card snapshot + verdict to `log/history.jsonl` (append-only, F1).
- Page renders: verdict timeline per asset, regime shifts, stance changes, F13 veto events, "model wrong/right" post-mortem notes (hand-written, sovereign-only).
- Weekly digest hash sealed to VAULT999 — the logbook inherits chain integrity instead of inventing its own.

---

## 6. Wireframes

**Terminal home `/wealth/`:**

```
┌──────────────────────────────────────────────────────────────┐
│ WEALTH ▮ Capital physics under sovereign judgment.   [F13 ●] │
│ DXY 103.2 · US10Y — · VIX — · Brent — · HH — · XAU —   (strip)│
├──────────────────┬──────────────────┬────────────────────────┤
│ GOLD  $4,063 ▲   │ OIL   $—    —    │ GAS   $—    —          │
│ MONETARY DEFENSE │ INDUSTRIAL FLOW  │ OPERATIONAL BALANCE    │
│ SABAR · 72%      │ verdict · conf   │ verdict · conf         │
│ trust: ▓▓▓░ calm │ choke: ▓░░░ open │ storage: 58% ▼ tight   │
├──────────────────┴──────────────────┴────────────────────────┤
│ CROSS-ASSET MATRIX   DXY↓ → Au+ · NG+ · Brent~   (one line/  │
│                      driver, computed from state.json)       │
├──────────────────────────────────────────────────────────────┤
│ LATEST VERDICTS (logbook tail, 5 rows)          → /wealth/log│
└──────────────────────────────────────────────────────────────┘
```

**Asset workspace (gold shown; oil/gas identical grammar, different Z4-first):**

```
┌──────────────────────────────────────────────────────────────┐
│ WEALTH / GOLD — MONETARY DEFENSE      [Gold|Oil|Gas]  [F13 ●]│
├──────────────────────────────────────────────────────────────┤
│ Z1 NOW ▮ $4,063.40 +2.10 (+0.05%)  MILD BULLISH 72%  SABAR   │
│    driver: USD weakening on soft CPI — safe-haven intact     │
├──────────────────────────────────────────────────────────────┤
│ Z2 MARKET STATE   [chart]   S1 4020 S2 3980 R1 4100 R2 4150  │
├──────────────────────────────────────────────────────────────┤
│ Z3 CAUSAL DRIVERS  monetary · geopolitics · flows · risk     │
├──────────────────────────────────────────────────────────────┤
│ Z4 RESERVE TRUST   real 10Y · CB buying · GSR · ETF · decay  │
├──────────────────────────────────────────────────────────────┤
│ Z5 DECISION GATE   [OBSERVE] [PREPARE] [AWAIT]  F13 veto ●   │
├──────────────────────────────────────────────────────────────┤
│ One causal chain. Three layers.        GEOX·arifOS·WEALTH    │
└──────────────────────────────────────────────────────────────┘
```

---

## 7. QQQ (recommendation discipline)

**Q1 — paths enumerated:**
A) Full SPA rebuild (React/Vite) · B) **Template + config static generation (recommended)** ·
C) Cosmetic differentiation only, keep 3 clones · D) NULL — do nothing ·
E) INVERSE — split further, one subdomain per asset · F) Absorb into AAA cockpit as tabs.

**Q2 — dominance:** B dominates on reversibility (static files, instant rollback), effort (~4.5% real divergence already isolated), ΔS (−6,000 lines), and agent parseability. A costs a build toolchain for pages that are fundamentally documents. C leaves the sed-clone drift bomb armed. E fights the review's core finding. F conflates cockpit (identity/A2A) with product surface — boundary violation.
**Q3 — precedent/interference:** the sed script already caused one drift class (gas nav still uses `--gold` CSS vars — visible in the diff). Superposition risk: none; static output means observed = served. Observer effect: agents already parse these pages (llms.txt exists) — schema must be a superset of `signal_v2.json`, never a break.

**RECOMMENDATION: path B, phased below.**

---

## 8. Implementation phases (all reversible until deploy)

| Phase | Work | Risk | Rollback |
|---|---|---|---|
| **P0** | `git init` the wealth subset of the webroot (or adopt into ARIF-SITES repo). **No mutation before version control** — closes the F11 gap that the live webroot is un-audited. | none | n/a |
| **P1** | Extract `_shell/` template + 3 configs; regenerate the 3 pages byte-comparable to today except the known drift bugs; delete `setup-commodity-dashboards.sh` (sed-clone dies) | low | git revert |
| **P2** | Terminal home: rebuild `/wealth/` body as tile grid + cross matrix + logbook tail | low | keep old index |
| **P3** | Differentiation: accents + first modules (Reserve Trust / Flow Map / Storage State) + new feeds (FRED, EIA — both free, no F13 budget trigger) | med | per-module flags |
| **P4** | `state.json` emitter + `/wealth/log/` + nightly cron + weekly VAULT999 digest seal | low | disable cron |

Deploy gate: each phase ships to `/var/www/html` only after local render check; public deploy = Tier 2 announce. Constitutional: F1 (append-only logbook, git-first), F4 (−6k lines), F7 (state.json carries `null` not fake numbers — current signal_v2 zeros violate this; v1 fixes it), F11 (webroot under git + VAULT999 digests), F13 (veto block stays first-class in Z5 and in schema).

---

*Blueprint only — no live files were mutated. Next action on approval: P0 + P1.*
