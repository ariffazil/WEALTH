/**
 * A-FORGE — Entropy Integrity Mesh build and test tools.
 *
 * These tools build, test, and validate the entropy integrity system.
 * They are NOT runtime judges — they are engineering instruments.
 */

export const AFORGE_ENTROPY_VERSION = "v1.0.0";
