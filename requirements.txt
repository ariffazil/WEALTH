# requirements.txt - WEALTH MCP Low-Entropy Capital Engine
numpy>=2.5.2
scipy>=1.10.0
numpy-financial>=1.0.0
supabase>=2.10.0
asyncpg>=0.29.0
# ── MCP stack — RECONCILED 2026-09-15 (KVM8) ────────────────────────────────
# Previously unpinned open ranges (`pydantic>=2.0.0`, `fastmcp>=3.2.4`) and
# `mcp` was absent entirely, even though WEALTH source imports the mcp SDK
# directly (`mcp.types.ToolAnnotations`, `mcp.server.fastmcp.FastMCP`).
# Pinned to the versions actually installed in /root/WEALTH/.venv.
# AUTHORITY: pyproject.toml + uv.lock remain the resolver authority
# (.github/dependabot.yml, ci-uv-lock-invariant.yml); this file is informational.
pydantic==2.13.4
fastmcp==3.4.5
mcp==1.29.0
httpx>=0.25.0
psycopg[binary]>=3.3.4
